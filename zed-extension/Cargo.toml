[package]
name = "fleet-gitops-zed"
version = "0.1.0"
edition = "2021"
description = "Zed extension for Fleet GitOps YAML validation"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"
